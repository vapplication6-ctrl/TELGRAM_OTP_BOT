BOT_TOKEN=
OWNER_ID=
UPI_ID=
DATABASE_URL=sqlite+aiosqlite:///./data/otpbot.db
ADMIN_IDS=

# Provider API keys. Keep these private and set them in your hosting environment.
NUMBEROTP_API_KEY=
VRNUM_API_KEY=

# Bot selling markup.
DEFAULT_MARKUP_RUPEES=15
RARE_MARKUP_RUPEES=30
RARE_COST_MAX_RUPEES=6

# Waiting order auto-expires after this many minutes (refund + try cancel)
ORDER_TIMEOUT_MINUTES=20

# No public website, Mini App, FastAPI server, webhook URL, or webhook secret is required.
# OTP delivery uses provider API polling from the Telegram bot process.
